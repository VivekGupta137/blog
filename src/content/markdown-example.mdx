export const metadata = {
  title: 'Example MDX File',
  description: 'An example MDX file demonstrating various code snippets and features.',
  keywords: ['MDX', 'example', 'code snippets'],
  authors: [{ name: 'John Doe', url: 'https://example.com' }],
}

# Example MDX File

This is an example MDX file that demonstrates various code snippets and features.


### Contents

### Quote
> This is a quote from the MDX file.

> asdsfs

<SuccessQuote>
custom SuccessQuote content
dasf

dsafdaf
</SuccessQuote>


<ErrorQuote>
custom ErrorQuote content
dasf

dsafdaf
</ErrorQuote>

<InfoQuote>
custom InfoQuote content
dasf

dsafdaf
</InfoQuote>

## Inline code

hello - `hello this is inline code` ddd

## Mermaid Diagram

```mermaid
    graph TD;
      A[Client Request]-->B[Server];
      A-->C[CDN];
      B-->D[Database];
      C-->D;
```

## PlantUML Diagram

```plantuml
@startuml
Alice -> Bob: Authentication Request
Bob --> Alice: Authentication Response
@enduml
```

## Server-Side Rendering Example

```javascript
// pages/blog/[id].js
export async function getServerSideProps(context) {
  const { params } = context;
  const data = await fetchData(params.id);
  
  return {
    props: {
      data,
    },
  };
}

export default function Page({ data }) {
  return (
    <div>
      <h1>{data.title}</h1>
      <p>{data.content}</p>
    </div>
  );
}
```

## Static Site Generation

```typescript
// lib/posts.ts
interface Post {
  id: string;
  title: string;
  content: string;
}

export async function getStaticProps(): Promise<{ props: { posts: Post[] } }> {
  const posts = await getAllPosts();
  
  return {
    props: {
      posts,
    },
    revalidate: 60, // Revalidate every 60 seconds
  };
}
```

## React Component Example

```jsx
// components/UserProfile.jsx highlight=6-7,15-17,26-30
import { useState, useEffect } from 'react';

export default function UserProfile({ userId }) {
  const [user, setUser] = useState(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    async function fetchUser() {
      try {
        const response = await fetch(`/api/users/${userId}`);
        const userData = await response.json();
        setUser(userData);
      } catch (error) {
        console.error('Failed to fetch user:', error);
      } finally {
        setLoading(false);
      }
    }

    fetchUser();
  }, [userId]);

  if (loading) return <div>Loading...</div>;
  if (!user) return <div>User not found</div>;

  return (
    <div className="user-profile">
      <img src={user.avatar} alt={user.name} />
      <h1>{user.name}</h1>
      <p>{user.bio}</p>
    </div>
  );
}
```

## Highlighted Example without Filename

```typescript
// highlight=1,3-5,8
interface ApiResponse<T> {
  data: T;
  success: boolean;
  message?: string;
  timestamp: number;
}

async function fetchData<T>(url: string): Promise<ApiResponse<T>> {
  const response = await fetch(url);
  return response.json();
}
```

## Long Line Example (Test Horizontal Scrolling)

```javascript
// test.js highlight=2
const shortLine = 'This is a short line';
const veryLongLineToTestHorizontalScrolling = 'This is a very long line that should definitely cause horizontal scrolling in the code block to test if the scrollbar appears and works correctly when content extends beyond the container width';
const anotherShortLine = 'Short';
```


## PlantUML big Diagram

```plantuml
@startuml
!theme toy

' Defines the main namespace for the e-commerce system
package "E-commerce System" {

  ' Abstract classes and interfaces
  abstract class AbstractUser {
    + String userId
    + String username
    + String email
    --
    + login()
    + logout()
  }

  interface Payable {
    + getPaymentAmount(): double
    + processPayment(): PaymentResult
  }

  interface Shippable {
    + getShippingAddress(): Address
    + getShippingWeight(): double
  }

  enum OrderStatus {
    PENDING
    PROCESSING
    SHIPPED
    DELIVERED
    CANCELLED
  }

  enum PaymentStatus {
    PENDING
    COMPLETED
    FAILED
    REFUNDED
  }

  ' Defines the core business entities
  package "Core Entities" {
    class Customer extends AbstractUser {
      + String firstName
      + String lastName
      + Address billingAddress
      + Address shippingAddress
      + List<Order> orders
      --
      + updateProfile()
      + viewOrderHistory()
    }

    class Order implements Payable {
      + String orderId
      + Date orderDate
      + Customer customer
      + OrderStatus status
      + List<LineItem> lineItems
      --
      + calculateTotal(): double
      + addLineItem(LineItem): void
    }

    class LineItem {
      + Product product
      + int quantity
      + double priceAtPurchase
      --
      + calculateSubtotal(): double
    }

    class Product implements Shippable {
      + String productId
      + String name
      + String description
      + double price
      + double weight
      + Category category
      --
      + getShippingAddress(): Address
      + getShippingWeight(): double
    }
  }

  ' Defines the supporting components
  package "Supporting Services" {
    class PaymentProcessor {
      + processCreditCardPayment(CreditCard): PaymentResult
      + processPayPalPayment(PayPalAccount): PaymentResult
    }

    class ShippingService {
      + generateShippingLabel(Order): ShippingLabel
      + trackShipment(String): ShipmentStatus
    }

    class NotificationService {
      + sendEmail(String to, String subject, String body)
      + sendSms(String to, String message)
    }

    class InventoryManager {
      + updateStock(Product, int change)
      + getStockLevel(Product): int
    }

    class SearchService {
      + searchProducts(String query): List<Product>
      + searchCategories(String query): List<Category>
    }
  }

  ' Defines the data structures and value objects
  package "Value Objects" {
    class Address {
      + String street
      + String city
      + String state
      + String zipCode
    }

    class CreditCard {
      - String cardNumber
      - Date expiryDate
      - String cvv
    }

    class PayPalAccount {
      - String email
    }

    class Category {
      + String name
    }

    class PaymentResult {
      + PaymentStatus status
      + String transactionId
    }
  }
}

' Establish relationships between the components

Customer "1" *-- "0..*" Order
Order "1" *-- "1..*" LineItem
LineItem "1" --> "1" Product
Product "0..*" -- "1" Category

Order ..> PaymentProcessor: uses >
Order ..> ShippingService: uses >
Order --> NotificationService: notifies >
Product ..> InventoryManager: manages >
Customer ..> SearchService: searches >

' Add some notes to further enrich the diagram
note right of Customer : A user who has made a purchase.
note top of Order : Represents a single transaction.
note bottom of PaymentProcessor : Handles all payment logic.
note "This is a detailed diagram showing\nmultiple components and their interactions." as N1
@enduml
```