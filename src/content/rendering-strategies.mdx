# Rendering Strategies in React

In this guide, we will explore different rendering strategies in React, including server-side rendering (SSR), static site generation (SSG), and client-side rendering (CSR). Understanding these strategies will help you choose the right approach for your application.

<Mermaid children={`
    graph TD;
      A-->B;
      A-->C;
      B-->D;
      C-->D;
`} />
